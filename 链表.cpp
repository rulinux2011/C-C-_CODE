#include<iostream>
using namespace std;

class list{
    struct Node{
        T data;
        Node* next;
        Node(const T& d=0):data(d),next(0){}

    };
    Node* head;
    public:
        List():head(NULL){}
};
int main(){
    List l;
    sizeof(l);
    return 0;
}
